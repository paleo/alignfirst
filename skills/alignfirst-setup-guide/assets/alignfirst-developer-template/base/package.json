{
  "name": "{{ADMIN_REPOSITORY_NAME}}",
  "private": true,
  "type": "module",
  "engines": { "node": ">=24.16.0" },
  "scripts": {
    "docmap": "npx -y alignfirst docmap",
    "workspace": "node scripts/workspace/workspace.mjs",
    "validate": "npx -y alignfirst docmap --check && node --check scripts/workspace/workspace.mjs"
  },
  "devDependencies": {
    "@paleo/workspace": "~0.32.0"
  }
}
