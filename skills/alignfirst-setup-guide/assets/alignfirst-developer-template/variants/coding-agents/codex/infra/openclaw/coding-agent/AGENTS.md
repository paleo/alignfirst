# AlignFirst Developer Service Account

Treat infrastructure, service, security, package, account, global-instruction, and runtime changes
as administration work. Follow the current admin repository and its `sysadmin` skill. Never expose,
copy, or automate interactive credentials.

For managed project work, use the project's instructions and AlignFirst protocols. Keep the OpenClaw
runtime provider independent from the delegated Codex selection.
