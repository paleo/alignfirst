.local
.local-wt
.plans
.env
node_modules
dist/
*.lock

.vscode/*
!.vscode/settings.example.json

alignfirst-developer-tests/.env.local
alignfirst-developer-tests/.codex-home/
alignfirst-developer-tests/artifacts/
alignfirst-developer-tests/.gateway-logs/
# Per-worker workspace copies, refreshed by `openclaw-test run`.
alignfirst-developer-tests/.workers/
# Locally-built @paleo/openclaw-* tarballs, regenerated by `npm run vendor`.
alignfirst-developer-tests/vendor/

# OpenClaw runtime state — recreated per-instance, not team-curated (a test
# instance snapshots these into the workspace; we don't track them)
alignfirst-developer-tests/workspace/.openclaw/
