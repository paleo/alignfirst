---
"@paleo/alcode": minor
---

The delegation guide now asks the agent to verify a completed run — project checks plus a hands-on look — rather than relaying the session file alone, and to run spec → plan → execute without stopping for the user to approve the plan.
