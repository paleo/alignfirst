---
"@paleo/alcode": minor
---

`alcode status` now accepts `--ticket <id>` or `--no-ticket` to reconcile the newest run in that scope. The OpenClaw guide locates the run's session file through scoped status and prohibits `process` calls on an alcode session.
