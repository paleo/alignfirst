---
"@paleo/alcode": minor
---

The CLI is now command-based: `alcode new`, `alcode resume <sessionId>`, `alcode status`. `new --protocol` requires `--ticket <id>` or the new `--no-ticket`, which reserves a `side-N` ticket. Added the `-m` alias for `--message`.
