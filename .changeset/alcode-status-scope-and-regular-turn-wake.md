---
"@paleo/alcode": minor
---

`alcode status` accepts `--ticket <id>` or `--no-ticket` to reconcile the newest run of that scope, and the OpenClaw guide chains the completion wake as `openclaw thread-handoff wake` without polling the run.
