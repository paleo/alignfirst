---
"@paleo/alcode": minor
---

`alcode status` accepts `--ticket <id>` or `--no-ticket` to reconcile the newest run of that scope, and the OpenClaw guide chains the completion wake as a regular `openclaw agent` turn without polling the run.
