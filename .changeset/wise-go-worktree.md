---
"@paleo/workspace": minor
---

Add `--go` to `workspace setup`: open an interactive shell in the new worktree (exit to return). Falls back to printing a `cd` hint when `$SHELL` is unset or stdin is not a tty.
