---
"@paleo/workspace": minor
---

Self-documenting CLI: `workspace --guide` and `dev --guide` print the full operating guide (every command, the directory layout, the guardrails), rendered in the project's package-manager syntax. Consumers point agents at the command instead of maintaining a separate `docs/workspace.md`.
