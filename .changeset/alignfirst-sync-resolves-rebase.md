---
"alignfirst": minor
---

`alignfirst sync` now resolves rebase conflicts in the shared plans clone by keeping the local version for content conflicts and every present path for rename and delete conflicts, while automatic archival skips running alcode session files.
