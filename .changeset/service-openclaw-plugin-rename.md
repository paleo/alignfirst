---
"@alignfirst/service-openclaw-plugin": minor
---

Renamed from `@paleo/alignfirst-developer-openclaw-plugin`, with plugin ID `alignfirst-service` and display name "AlignFirst Service". Update `plugins.allow` and `plugins.entries` in `openclaw.json` to the new ID. Thread-handoff state under `~/.openclaw/thread-handoff/` is unaffected.
