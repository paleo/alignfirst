---
"@paleo/alcode": minor
---

The `--model` flag now takes a bare model name from a fixed list: `fable`, `opus`, `sonnet`, `haiku`. Set `ALIGNFIRST_CODE_MODELS` (comma-list) to replace the list, e.g. on a host whose plan lacks some tiers.
