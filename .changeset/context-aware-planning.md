---
"@paleo/alcode": minor
"@paleo/openclaw-test": minor
---

Renamed `alcode usage` to `alcode quota`, and added the run's context-window occupancy as `contextTokens` in the session file and in `alcode status`. The delegation guide now uses it to decide whether the plan protocol continues in the spec's session or starts in a fresh one from a self-sufficient spec.
