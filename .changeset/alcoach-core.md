---
"@paleo/alcoach": minor
---

New `alcoach` CLI: coach a coding agent through AlignFirst protocols. Runs stream a live transcript to a per-call log file under `.plans/`; when a callback URL is configured it runs in the background and calls back on completion, otherwise it tails the run in the foreground.
