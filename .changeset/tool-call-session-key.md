---
"@paleo/openclaw-test": minor
---

Agent tool calls now carry the `sessionKey` of the session that made them, and `ScenarioContext` gains `getAgentToolCalls()` for one-shot sweeps over the trajectory.
