# About This Project

This is **not** a real product. It's a minimal monorepo materialized by the OpenClaw playbook tests to exercise the coding-agent workflow end to end: workspace setup, dev-server lifecycle, branching, and conventions.

Two copies run side by side; only the package name and the entry-point title diverge. Treat it as scaffolding — there's nothing here worth studying for its own sake.
