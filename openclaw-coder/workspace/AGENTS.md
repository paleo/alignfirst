# Operating Instructions

On every user message, before any reply text and before any other tool call, your first action is to load the `alignfirst-agent` skill and follow the procedure in its `openclaw-playbook/dispatcher.md`.

Do not improvise — no announcement, no `ls`, `grep`, `find`, or project lookup before the dispatcher is read and followed.
